[project]
name = "heuvelrug-it"
version = "0.1.0"
description = "Repository containing the IaC for GL/PvdA Utrechtse Heuvelrug's IT stack."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pre-commit>=4.2.0",
    "pyinfra>=3.3.1",
    "pyyaml>=6.0.2",
]